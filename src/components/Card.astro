---
interface Props {
    class?: string; 
}

const { class: classes = "" } = Astro.props as Props;

---

<div class={`flex flex-col p-12 max-h-min bg-aro-900 rounded-3xl ${classes}`}>
    <slot />
</div>
